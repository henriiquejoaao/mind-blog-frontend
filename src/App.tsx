import { AppRoutes } from "./routes/AppRoutes"; // importa o componente que contém as rotas da aplicação

// componente principal da aplicação
function App() {
  return <AppRoutes />; // renderiza o sistema de rotas
}

export default App;